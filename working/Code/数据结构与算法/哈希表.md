### 哈希表概念
  哈希一般使用场景是什么，了解吗？

### 应用场景
- 数字签名中用于验证数据的**完整性**（哈希摘要算法）
- 数字证书--`CA`
- 密码存储
- 快速检索数据
- 快速比较两个数
- 去重

### 数组去重
- *哈希表*：遍历数组，将每个元素作为键值存储，后将键值转换为数组
- *集合*：集合是一个只存储唯一元素的数据结构
- *排序后去重*：双指针
- *双层循环*