### 小细节
- 先判断，后移动（先检查是否可以做/满足条件，而不是做完再悔棋）
- 尽量不要让一个变量承担多个作用，需特别注意
- double  
- `x>>=1`而不是`x>>1`
#### 4.int函数的括号
^8c3c8f
```c++
int fn(){
}
int main()
{
cout<<fn()<<endl;//!!!!!!!括号不能丢
} 

`````

#### 5.仔细读题
第一行包含一个整数 n ，表示农场个数。
接下来 n+1 行，每行包含 n 个整数，输入一个对角线上全是0的对称矩阵。  
其中第 x+1 行 y 列的整数表示连接农场 x 和农场 y 所需要的光纤长度。
```c++
for(int i=1;i<=n;i++)
		for (int j = 1; j <= n; j++)
		{
			int x;
			cin >> x;
			g[i][j]=g[j][i]  =min(g[i][j], x);
			 
		}

错误步骤！！！g[i-1][j]=x;
```


### 多次调用函数返回值，先预存
[[并查集&acwing连通块中的数量#^872140]]



### 边界问题
[[第五届传智杯 c题#未通过]]

### 模运算
模运算与基本四则运算有些相似，但是除法例外。其规则如下：
(a + b) % p = (a % p + b % p) % p （1）
(a - b) % p = (a % p - b % p ) % p （2）
(a * b) % p = (a % p * b % p) % p （3）
a ^ b % p = ((a % p)^b) % p （4）

先模和后模的结果是一样的，先取模可以保证中间结果不会溢出

### 不可用右移代替负数除以2的整数幂
![[移位运算.png]]


### 类型转换
float double——>int   1.向零舍入  
                                    2.发生溢出
#### 预处理指令
#if`，`#else`和`#endif`之间的关系是它们一起用于控制代码的条件编译。`#if`后面跟一个常量表达式来决定哪个代码块会被编译和执行，`#else`用于指定当`#if`的条件为假时执行的代码，`#endif`用于标记条件编译的结束。

#### 枚举
在 C 和 C++ 中，当您定义 时`enum`，第一个枚举数的值为`0`，并且后续每个枚举数的值都比其前一个枚举数的值大 1。`I2C_MASTER_WRITE`如果您显式地为枚举器设置一个值（如示例中所示） ，则后续枚举器将​​从该值继续。

## `t++`
```c
while(t--)
{
}比较的是t--之后的值
```