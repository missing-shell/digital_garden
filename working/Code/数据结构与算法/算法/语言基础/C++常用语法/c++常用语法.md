1.system("pause");//请按任意键继续  system("cls");//清屏操作
2.（ ）？1：2;//条件满足为1，不满足为2
3.printf("\n")的速度比cout<<endl快的多
4..
```c++
#初始化
#include<string.h>
memeset(a,1,sizeof(a))   //全部初始化为1,通常只能初始化为0 或 -1，其余数容易出错
int a[40]={0};     //初始化为0，特殊方法