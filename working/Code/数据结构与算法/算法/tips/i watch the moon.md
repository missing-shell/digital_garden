学习算法时应该多动笔模拟，不要总是感觉理解的不够透彻。


```c++
cin>>ans[i].x>>ans[i].y>>ans[i].z;
//可是先输入x,y,z。在所有处理之后，在使用ans[i]={x,y,z}
```

比较距离时，可以两边同时平方，尽量不要调用函数

注意题目提示，`坐标绝对值小于1e9`，即使坐标小于0 无意义，但这句话也说明坐标是可能小于零的。使用`abs`及加减运算时，应注意运算和使用`abs`的顺序。

如果有许多if判断特例，需要思考一种新方式压缩一下，合并某些特例判断

特别注意题目中的所有数据均在`int`范围，很有可能是在最小值和最大值之间，可用`-0x80000000`表示最小值。

代码报错，逻辑没有问题
	[[AcWing899编辑距离]]
		检查`<` 和`<=`
		检查取值范围
		检查多组询问是否及时初始化相关数据