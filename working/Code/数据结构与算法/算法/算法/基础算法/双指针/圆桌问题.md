```c++

#include<iostream>
#include<algorithm>
#include<vector>
using namespace std;
 
int main()
{
	int n, m,leap=0;
	while (scanf_s ("%d%d", &n, &m) != EOF) {
		leap++;
		vector<int> ans;
		for (int i = 0; i <= 2 * n; i++) {
			ans.push_back(0);
		}
		int cnt = 0,sum=0, i =1,j;
		while (sum != n) {
			if (!ans[i])cnt++;
			if (cnt == m) {
				ans[i] = 1;
				cnt = 0;
				sum++;
			}
			i %= 2 * n;
			i++;
		}
		int count = 0;
		for (int i = 1; i < ans.size(); i++) {
			if (count && count % 50 == 0)cout << endl;
			if (!ans[i])cout << 'G';
			else cout<<'B';
			count++;
		}
		cout << endl << endl;
		 
	}
	return 0;

}
```
两个指针各司其职。