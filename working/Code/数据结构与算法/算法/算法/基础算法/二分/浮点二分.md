```c++
if (n < 0) { cout << "-"; n = n * -1; }
    double l = 0, r = n;
    if (n < 1)r = 1;
    double mid;
    while (r - l > 1e-8)
    {
        mid = (l + r) / 2;
        if (mid * mid * mid < n)l = mid;
        else r = mid;
    }
    注意边界以及浮点数的精度问题。
    小数的平方根大于它本身
    double l=-1000,r=1000;更优的解决方案
```
    
