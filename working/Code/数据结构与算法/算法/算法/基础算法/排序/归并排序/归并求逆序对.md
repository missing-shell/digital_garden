```c++

```while (i <= mid && j <= r) {
		if (ans[i] <= ans[j])tmp[k++] = ans[i++];
		else tmp[k++] = ans[j++],cnt+=(mid-i+1);
	}
	三种情况，都在mid左边
	                  都在mid右边
	                   一左一右
	并且两边排序不会影响第三种结果。
    因为递归，可以将一二种情况当作第三种来求，保证累加即可
    ```