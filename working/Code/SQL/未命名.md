* and的优先级大于or

* between [下届] and [上届] _**(包含上下界）

* order by 的位置位于所有语句的倒数第二，倒数第一是 limit*_

* like 关键字查找，“%y”意味着查找开头有y的记录，“y%”意味着查找结尾有y的记录，“%toy%”意味着查找记录中有toy的记录

- 最大值---max()
- 最小值---min()
- 平均值---avg()
- 总值 ---sum()
- 总数 ---count()
* count(\*) 查全部的记录条数 
* count(数字)，和count(\*)效果一样，有几条记录就显示几
* count(属性名)则查处的是属性名下面的非null数据的个数
* count()则报错


* 关系数据库中用二维表来表示实体之间的联系
* 数据库模型分为：层次模型、网状模型、关系模型、面向对象模型。
* 数据模型可分为：概念数据模型、逻辑数据模型和物理数据模型三种；E-R图属于概念数据模型一种，而关系数据模型属于逻辑数据模型的一种

*  复合索引可以只使用复合索引中的一部分，但必须是由最左部分开始，且可以存在常量。 
* 若关系中某一属性的值能唯一标识一个元组，而其子集不能，则称该元组为候选码。关系中若有多个候选码，选定一个为主码。
* 应尽量避免在 where 子句中使用 or 来连接条件，否则将导致引擎放弃使用索引而进行全表扫描，
* Union因为要进行重复值扫描，所以效率低。如果合并没有刻意要删除重复行，那么就使用Union All 
* **数据库管理系统是系统软件，而数据库系统和数据库应用系统都是应用软件**。

* [投影](https://www.baidu.com/s?wd=%E6%8A%95%E5%BD%B1&tn=SE_PcZhidaonwhc_ngpagmjz&rsv_dl=gh_pc_zhidao)是选取相应列组成新[元组](https://www.baidu.com/s?wd=%E5%85%83%E7%BB%84&tn=SE_PcZhidaonwhc_ngpagmjz&rsv_dl=gh_pc_zhidao)，连接在多个关系的[笛卡尔积](https://www.baidu.com/s?wd=%E7%AC%9B%E5%8D%A1%E5%B0%94%E7%A7%AF&tn=SE_PcZhidaonwhc_ngpagmjz&rsv_dl=gh_pc_zhidao)中进行筛选出的新关系，交运算是多个关系之间交集所产生的新的关系。

 