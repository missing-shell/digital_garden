## 问题
/usr/bin/gcc -fdiagnostics-color=always -g /home/shell/Project/NJU_OS_Project/hanoi/hanoi-main.c -o /home/shell/Project/NJU_OS_Project/hanoi/hanoi-main
/usr/bin/ld: /tmp/ccP3nUv6.o: in function `main':
/home/shell/Project/NJU_OS_Project/hanoi/hanoi-main.c:12: undefined reference to `hanoi'
collect2: error: ld returned 1 exit status
## 解决方式你遇到的问题是链接器（`ld`）在尝试链接你的程序时找不到 `hanoi` 函数的定义。这通常发生在以下几种情况：

1. **函数未定义**：你的程序中调用了 `hanoi` 函数，但是在编译命令中没有包含定义这个函数的源文件。
2. **函数定义在动态库中**：如果 `hanoi` 函数是在一个动态库（`.so` 文件）中定义的，你需要在链接时指定这个库。
3. **函数定义在静态库中**：如果 `hanoi` 函数是在一个静态库（`.a` 文件）中定义的，你需要在链接时指定这个库。

### 解决方法

- **确保所有源文件都被编译**：如果 `hanoi` 函数的定义在 `hanoi-main.c` 文件之外的其他源文件中，你需要确保在编译命令中包含这些源文件。例如，如果 `hanoi` 函数在 `hanoi.c` 文件中定义，你应该使用以下命令：

 ```bash
 /usr/bin/gcc -fdiagnostics-color=always -g /home/shell/Project/NJU_OS_Project/hanoi/hanoi-main.c /home/shell/Project/NJU_OS_Project/hanoi/hanoi.c -o /home/shell/Project/NJU_OS_Project/hanoi/hanoi-main
 ```

- **链接动态库**：如果 `hanoi` 函数在一个动态库中定义，你需要在链接时指定这个库。例如，如果 `hanoi` 函数在 `libhanoi.so` 库中，你应该使用以下命令：

 ```bash
 /usr/bin/gcc -fdiagnostics-color=always -g /home/shell/Project/NJU_OS_Project/hanoi/hanoi-main.c -o /home/shell/Project/NJU_OS_Project/hanoi/hanoi-main -L/path/to/library -lhanoi
 ```

  其中，`-L` 选项指定库的路径，`-l` 选项指定库的名称（不包括 `lib` 前缀和 `.so` 后缀）。

- **链接静态库**：如果 `hanoi` 函数在一个静态库中定义，你需要在链接时指定这个库。例如，如果 `hanoi` 函数在 `libhanoi.a` 库中，你应该使用以下命令：

 ```bash
 /usr/bin/gcc -fdiagnostics-color=always -g /home/shell/Project/NJU_OS_Project/hanoi/hanoi-main.c /path/to/libhanoi.a -o /home/shell/Project/NJU_OS_Project/hanoi/hanoi-main
 ```

  这里，你直接在命令行中指定静态库文件的路径。

请根据你的具体情况选择合适的解决方法。如果 `hanoi` 函数的定义确实在 `hanoi-main.c` 文件中，那么可能是编译命令中缺少了某些源文件。如果函数在库中定义，确保正确地链接了这些库。