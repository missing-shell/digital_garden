用数学的方式解释一下为什么是大间距的分类器
机器学习泛化的性能 什么东西决定上下限
过拟合 欠拟合
三类集合分别干什么用 
```
神经网络代价函数是非凸的
```
假设函数的表达式 单变量线性回归的表达式 
多变量的xji i 第i个样本 j 第i个样本里的第j个特征
混淆矩阵 写出来 二分类
查准率和查全率的公式 **查准是预测是真实的数量除以真实正确的数量**
学习率是动态变化还是固定数字
正则化 回归问题梯度下降的表达式 用梯度下降发求θ 是回归的还是分类的

分析题 评价分类算法 查准查全 auc、roc 曲线 排序损失怎么求 roc曲线怎么画
```
特征工程的方式 方差法 迭代法 前向后向 看哪一些特征有用
```

机器学习的建议 遇到一个测试集误差很大的模型 该怎么做 看学习曲线

## 滴答清单
选择：三类集合分别干什么用、范化性能、上限

**线性回归**

逻辑回归代价表达式--理解

```
神经网络为什么i从1开始，j从0开始
```

AUC与排序损失之间的关系--例子验证排序损失

模型有问题--怎么做

机器学习建议--分类  （例如统一大小写）

应对欠拟合 过拟合问题的方法
机器学习的上限和难度
支持向量🐔为什么是大间距：满足代价最小
激活单元
高斯函数
## 参考
1.机器学习过程四个要素:训练集，测试集，算法，模型
2.机器学习分为:监督学习（分类，回归问题），非监督，半监督（区别是有无标识）
3.训练集作用:训练模型，得出决策边界θ;测试集:评价模型好坏;验证集:调参，选出误差min的参数，调模型
4.查准率，查全率公式
5.**手写数字识别的例题**（画ROC曲线和计算AUC）
6.梯度下降法不需要每一轮迭代变化α，每一次迭代会变慢速度
7.单变量线性回归，多变量线性回归梯度下降法公式
8.梯度下降法和正
规方程法对比
9.逻辑回归g(z)=…
10.逻辑回归从自变量的角度进行决策，决策边界用于决策测试集，决策边界参数θ由训练集获得
11.逻辑回归cost(hθ(x)y)公式


12.正则化解决过拟合问题，正则化线性回归表达式，加了正则化的梯度下降法表达式
```

13.神经网络公式中各个脚标意义， 用神经网络做逻辑门
14.神经网络代价函数各个脚标意义
15.反向传播公式各脚标意义
16.神经网络模型训练步骤
```

17.欠拟合，过拟合时有何特征（训练集误差，验证集误差，偏差大还是方差大）以及如何解决欠拟合过拟合问题
18.利用学习曲线来分辨模型遭受什么问题
19.利用邮件中大小写，发件人，以及各种标点符号和链接来判断是不是垃圾邮件
20.机器学习上限是样本和特征，机器学习难度在于偏差，方差和学习本身的难度，模型与算法无限逼近上限
21.会举例说出预处理和特征选择的几种方法

```
22.支持向量积Z—〉正无穷/负无穷，hθ（X）趋近于1/0，cost趋近于0
23.参数θ就是支持向量积假设函数形式，假设函数hθ（x）
24.解释为什么支持向量积适用于大间距
```


```
25.分堆问题:K-means算法，（有可能局部最优解）肘部法则缺点聚类问题簇个数，根据实际工程选择K，J（C1，C2）公式
```
